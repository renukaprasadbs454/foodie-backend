package com.foodie.admin.entity;

/**
 * Binding admin sub-roles (Phase3 §3.8 / §5.2). Exact enum: SUPER_ADMIN, OPS, FINANCE, SUPPORT.
 */
public enum AdminRoleName {
    SUPER_ADMIN,
    FINANCE_ADMIN,
    OPERATIONS_ADMIN,
    RESTAURANT_MANAGER,
    SUPPORT_AGENT,
    AUDITOR,
    OPS,
    FINANCE,
    SUPPORT,
    DARKSTORE_ADMIN
}
